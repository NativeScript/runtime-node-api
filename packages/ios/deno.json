{
  "tasks": {
    "build": "deno run -A ../../scripts/build.ts ios-universal"
  },

  "fmt": {
    "exclude": [
      "build",
      "node_modules",
      "types"
    ]
  },

  "lint": {
    "exclude": [
      "build",
      "node_modules",
      "types"
    ]
  },

  "imports": {
    "@nativescript/ios-node-api": "./mod.ts"
  }
}
